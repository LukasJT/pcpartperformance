module.exports=({guides,guideIcon,categoryPaths,esc})=>{
 const titles={gpu:'How to choose a graphics card',cpu:'How to choose a processor',ram:'Understanding PC memory',ssd:'Choosing an SSD'};
 const index=`<header class="learn-intro"><span class="section-label">Learn</span><h1>Understand your hardware.</h1><p>Clear explanations of the specifications, compatibility checks, and tradeoffs behind your next upgrade.</p></header><div class="learn-layout"><section class="learn-stories" aria-labelledby="guide-list-title"><h2 id="guide-list-title">Hardware essentials</h2>${guides.map(g=>`<article class="learn-story">${guideIcon(g.cat)}<div><span class="section-label">${g.title}</span><h3><a href="/learn/${g.cat}/">${titles[g.cat]}</a></h3><p>${g.subtitle}</p><div class="story-topics">${g.terms.map(t=>`<span>${esc(t[0])}</span>`).join('')}</div></div></article>`).join('')}</section><aside class="learn-sidebar"><h2>Put it into practice</h2><a href="/compare/"><strong>Compare two components</strong><span>See specifications side by side.</span></a><a href="/builder/"><strong>Plan your PC</strong><span>Check how your parts fit together.</span></a><a href="/performance/"><strong>Explore game performance</strong><span>PC estimates and console graphics modes.</span></a><a href="/hardware/"><strong>Find a specific model</strong><span>Browse the hardware catalog.</span></a></aside></div>`;
 const article=g=>`<nav class="article-breadcrumb" aria-label="Breadcrumb"><a href="/learn/">Hardware guides</a><span aria-hidden="true">/</span><span>${g.title}</span></nav><header class="article-header"><span class="section-label">${g.title} guide</span><h1>${titles[g.cat]}</h1><p>${g.subtitle}</p></header><div class="article-layout"><nav class="article-toc" aria-label="On this page"><strong>On this page</strong>${g.terms.map((t,i)=>`<a href="#topic-${i+1}">${esc(t[0])}</a>`).join('')}<a href="#next-step">Next step</a></nav><article class="article-copy">${g.terms.map(([term,explanation],i)=>`<section id="topic-${i+1}"><h2>${esc(term)}</h2><p>${esc(explanation)}</p></section>`).join('')}<section class="article-next" id="next-step"><h2>Compare your options</h2><p>Use these specifications to narrow your choices, then check the details for the exact model.</p><div><a class="button primary" href="/compare/?category=${g.cat}">Compare ${g.title.toLowerCase()}</a><a class="button secondary" href="/hardware/${categoryPaths[g.cat]}/">Browse ${g.title.toLowerCase()}</a></div></section><nav class="related-guides" aria-label="More hardware guides"><h2>Keep reading</h2>${guides.filter(x=>x.cat!==g.cat).map(x=>`<a href="/learn/${x.cat}/">${titles[x.cat]}</a>`).join('')}</nav></article></div>`;
 return {index,article};
};
