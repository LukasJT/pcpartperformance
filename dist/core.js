'use strict';
const icons={gpu:'<rect x="3" y="5" width="18" height="12" rx="1"/><circle cx="10" cy="11" r="3"/><path d="M3 17v3m4-3v3m4-3v3m8-11h2m-2 4h2"/>',cpu:'<rect x="5" y="5" width="14" height="14" rx="1"/><rect x="9" y="9" width="6" height="6"/><path d="M8 2v3m4-3v3m4-3v3M8 19v3m4-3v3m4-3v3M2 8h3m-3 4h3m-3 4h3m14-8h3m-3 4h3m-3 4h3"/>',board:'<rect x="4" y="2" width="16" height="20" rx="1"/><rect x="7" y="5" width="6" height="6"/><path d="M16 5v9M7 15h6m-6 3h10"/>',ram:'<rect x="2" y="7" width="20" height="10" rx="1"/><path d="M5 10v4m4-4v4m6-4v4m4-4v4M4 17v3m4-3v3m4-3v3m4-3v3m4-3v3"/>',ssd:'<rect x="2" y="7" width="20" height="10" rx="1"/><rect x="6" y="10" width="5" height="4"/><path d="M15 10h3m-3 4h3"/>',hdd:'<rect x="4" y="2" width="16" height="20" rx="1"/><circle cx="12" cy="10" r="5"/><path d="m12 10 5 7M7 19h3"/>',psu:'<rect x="3" y="4" width="18" height="16" rx="1"/><circle cx="10" cy="12" r="5"/><path d="M18 8v3m0 3v2"/>',case:'<path d="M6 2h12v20H6zM9 5h6v5H9z"/><circle cx="12" cy="16" r="3"/>',monitor:'<rect x="2" y="3" width="20" height="14" rx="1"/><path d="M8 21h8m-4-4v4"/>',mouse:'<path d="M12 2a7 7 0 0 0-7 7v6a7 7 0 0 0 14 0V9a7 7 0 0 0-7-7zM12 2v7m-7 1h14"/>',keyboard:'<rect x="2" y="5" width="20" height="14" rx="1"/><path d="M5 9h1m3 0h1m3 0h1m3 0h1M5 13h1m3 0h1m3 0h1m3 0h1M7 16h10"/>',microphone:'<rect x="9" y="2" width="6" height="12" rx="3"/><path d="M6 11a6 6 0 0 0 12 0m-6 6v4m-4 0h8"/>',headset:'<path d="M4 14v-3a8 8 0 0 1 16 0v3M4 13h4v7H5a1 1 0 0 1-1-1zm16 0h-4v7h3a1 1 0 0 0 1-1z"/>',controller:'<path d="M7 8h10a5 5 0 0 1 4 6l-1 5a2 2 0 0 1-3 1l-3-3h-4l-3 3a2 2 0 0 1-3-1l-1-5a5 5 0 0 1 4-6zM7 12v4m-2-2h4m7-1h.01m2 2h.01"/>',console:'<rect x="7" y="2" width="10" height="20" rx="1"/><path d="M10 6h4m-4 3h4m-4 8h4"/>',phone:'<rect x="6" y="2" width="12" height="20" rx="2"/><path d="M10 5h4m-3 14h2"/>',handheld:'<rect x="2" y="6" width="20" height="12" rx="3"/><rect x="7" y="8" width="10" height="8"/><path d="M4 11v2m-1-1h2m14 0h1"/>',document:'<path d="M5 2h10l4 4v16H5zM15 2v5h4M8 11h8m-8 4h8m-8 3h5"/>',chart:'<path d="M3 3v18h18M7 16v-5m5 5V7m5 9V4"/>',compare:'<path d="M3 7h18m-4-4 4 4-4 4M21 17H3m4-4-4 4 4 4"/>',all:'<rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/>'};
const icon=(name)=>`<svg viewBox="0 0 24 24" aria-hidden="true">${icons[name]||icons.cpu}</svg>`;
const categories=[['gpu','Graphics cards'],['cpu','Processors'],['board','Motherboards'],['ram','Memory'],['ssd','SSDs'],['hdd','Hard drives'],['psu','Power supplies'],['case','PC cases'],['monitor','Monitors'],['mouse','Mice'],['keyboard','Keyboards'],['microphone','Microphones'],['headset','Headsets'],['controller','Controllers'],['console','Consoles'],['phone','Phones'],['handheld','Handhelds']];
const colors=['#fc8450','#6eb2f1','#b7cf80','#c5a1df'];
const catalog=[
{id:'rtx4080s',name:'GeForce RTX 4080 SUPER',brand:'NVIDIA',cat:'gpu',year:2024,architecture:'Ada Lovelace',summary:'16 GB GDDR6X',specs:{'Memory':16,'Memory bandwidth':736,'Boost clock':2.55,'Board power':320,'Shader units':10240,'PassMark G3D':34212,'Expansion slots':3,'Power connectors':1},source:'https://www.nvidia.com/en-us/geforce/graphics-cards/40-series/rtx-4080-family/'},
{id:'rx7900xtx',name:'Radeon RX 7900 XTX',brand:'AMD',cat:'gpu',year:2022,architecture:'RDNA 3',summary:'24 GB GDDR6',specs:{'Memory':24,'Memory bandwidth':960,'Boost clock':2.5,'Board power':355,'Shader units':6144,'Expansion slots':3,'Power connectors':2},source:'https://www.amd.com/en/products/graphics/desktops/radeon/7000-series/amd-radeon-rx-7900xtx.html'}
];
catalog.push(...window.PCP_EXTRA);
catalog.push(...window.PCP_HISTORY);
catalog.push(...window.PCP_UNIVERSE.filter(p=>!catalog.some(existing=>existing.name.toLowerCase()===p.name.toLowerCase())));
catalog.push(...window.PCP_COMPONENTS.filter(p=>!catalog.some(existing=>existing.name.toLowerCase()===p.name.toLowerCase())));
const units={'Memory':'GB','Memory bandwidth':'GB/s','Boost clock':'GHz','Base clock':'GHz','Clock speed':'MHz','Data width':'bit','External bus width':'bit','Transistors':'','Board power':'W','Shader units':'','PassMark G3D':'points','CPU cores':'','CPU threads':'','GPU cores':'','GPU FP32':'TFLOPS','L3 cache':'MB','CPU TDP':'W','DIMM slots':'','M.2 slots':'','SATA ports':'','Max RAM':'GB','Transfer rate':'MT/s','CAS latency':'cycles','Modules':'','Storage capacity':'GB','Sequential read':'MB/s','Sequential write':'MB/s','Endurance':'TBW','Spindle speed':'RPM','Drive cache':'MB','Rated output':'W','Fan size':'mm','Display refresh':'Hz','Battery capacity':'Wh','Display size':'in'};
let selected=['rtx4080s','rx7900xtx'],activeCategory='gpu',catalogCategory='all',metric='Memory bandwidth',visibleRows=7;
const buildSlotDefs=[
 {id:'cpu',label:'Processor',required:true},{id:'board',label:'Motherboard',required:true},{id:'gpu',label:'Graphics card',required:false},
 {id:'ram',label:'Memory kit',required:true},{id:'ssd',label:'Solid-state drive',required:false},{id:'hdd',label:'Hard drive',required:false},{id:'psu',label:'Power supply',required:true},{id:'case',label:'PC case',required:true}
];
const multiBuildSlots=new Set(['ram','ssd','hdd']);
const starterBuild={cpu:'9800x3d',board:'b650',gpu:'rtx4080s',ram:['ddr5'],ssd:['990pro'],hdd:['barracuda2'],psu:'rm850x',case:'x-case-fractal-design-north'};
const cloneStarter=()=>Object.fromEntries(Object.entries(starterBuild).map(([slot,value])=>[slot,Array.isArray(value)?[...value]:value]));
let pcBuild={},compatibleOnly=true;
const $=(s)=>document.querySelector(s);
function parts(){return selected.map(id=>catalog.find(p=>p.id===id)).filter(Boolean)}
function number(v){return v===undefined||v===null?'—':v.toLocaleString('en-US',{maximumFractionDigits:2})}
function value(p,key){return p.specs[key]===undefined?'—':`${number(p.specs[key])}${units[key]?' '+units[key]:''}`}
function buildIds(slot,build=pcBuild){const value=build[slot];return Array.isArray(value)?value.filter(Boolean):value?[value]:[]}
function buildParts(slot){return buildIds(slot).map(id=>catalog.find(p=>p.id===id&&p.cat===slot)).filter(Boolean)}
function buildPart(slot){return buildParts(slot)[0]}
function socketOf(p){return p?.architecture.match(/\b(SUPER SOCKET 7|SOCKET\s?(?:1|2|3|4|5|7|8|370|423|462|478|754|939|940)|SLOT\s?[12A]|AM[2345]\+?|SP[35]|TR4|sTRX4|sTR5|sWRX8|LGA\d{3,4})\b/i)?.[1].replace(/\s+/g,' ').toUpperCase()}
function ddrOf(p){return `${p?.architecture||''} ${p?.summary||''}`.match(/\bDDR[345]\b/i)?.[0].toUpperCase()}
function formFactorOf(p){const text=`${p?.architecture||''} ${p?.summary||''}`.toUpperCase();if(/MINI-ITX|MINI ITX/.test(text))return'MINI-ITX';if(/MICRO-ATX|MICRO ATX|MATX/.test(text))return'MICRO-ATX';if(/E-ATX|EATX/.test(text))return'E-ATX';if(/\bATX\b/.test(text))return'ATX'}
function caseSupports(chassis,board){if(!chassis||!board)return true;const factor=formFactorOf(board),support=`${chassis.architecture} ${chassis.summary}`.toUpperCase().replaceAll('MATX','MICRO-ATX');if(!factor)return false;return support.includes(factor)||(factor==='MICRO-ATX'&&support.includes('ATX'))||(factor==='MINI-ITX'&&(support.includes('ATX')||support.includes('MICRO-ATX')))}
function candidateCompatible(slot,candidate){
 const single=id=>slot===id?candidate:buildPart(id),cpu=single('cpu'),board=single('board'),gpu=single('gpu'),psu=single('psu'),chassis=single('case');
 const group=id=>{const current=buildParts(id);return slot===id&&!current.some(p=>p.id===candidate.id)?[...current,candidate]:current},rams=group('ram'),ssds=group('ssd'),hdds=group('hdd');
 const socketMatch=()=>!cpu||!board||Boolean(socketOf(cpu)&&socketOf(board)&&socketOf(cpu)===socketOf(board));
 const memoryMatch=()=>{if(!board||!rams.length)return true;const boardDDR=ddrOf(board),generations=rams.map(ddrOf);if(!boardDDR||generations.some(v=>!v||v!==boardDDR))return false;const capacity=rams.reduce((sum,p)=>sum+(p.specs.Memory||0),0),modules=rams.reduce((sum,p)=>sum+(p.specs.Modules||0),0);return(board.specs['Max RAM']===undefined||capacity<=board.specs['Max RAM'])&&(board.specs['DIMM slots']===undefined||modules<=board.specs['DIMM slots'])};
 const storageMatch=()=>{if(!board)return true;if(hdds.some(p=>!/SATA/i.test(p.architecture)))return false;if(ssds.some(p=>!/(NVMe|SATA)/i.test(p.architecture)))return false;const nvme=ssds.filter(p=>/NVMe/i.test(p.architecture)).length,sata=hdds.filter(p=>/SATA/i.test(p.architecture)).length+ssds.filter(p=>/SATA/i.test(p.architecture)).length;return(board.specs['M.2 slots']===undefined||nvme<=board.specs['M.2 slots'])&&(board.specs['SATA ports']===undefined||sata<=board.specs['SATA ports'])};
 const powerMatch=()=>{if(!psu)return true;const rated=psu.specs['Rated output'],cpuW=cpu?.specs['CPU TDP'],gpuW=gpu?.specs['Board power'];if(rated===undefined||cpuW===undefined||(gpu&&gpuW===undefined))return true;return rated>=Math.ceil((cpuW+(gpuW||0)+100)*1.25/50)*50};
 if(slot==='cpu')return socketMatch()&&powerMatch();if(slot==='board')return socketMatch()&&memoryMatch()&&storageMatch()&&caseSupports(chassis,board);if(slot==='ram')return memoryMatch();if(slot==='ssd'||slot==='hdd')return storageMatch();if(slot==='gpu'||slot==='psu')return powerMatch();if(slot==='case')return caseSupports(chassis,board);return true;
}
function percentileScore(value,values){if(value===undefined||!values.length)return null;const sorted=values.filter(Number.isFinite).sort((a,b)=>a-b);return Math.round(25+70*(sorted.filter(v=>v<=value).length/Math.max(sorted.length,1)))}
function checkResult(state,label,detail){return{state,label,detail}}
function analyzeBuild(){
 const cpu=buildPart('cpu'),board=buildPart('board'),gpu=buildPart('gpu'),rams=buildParts('ram'),ssds=buildParts('ssd'),hdds=buildParts('hdd'),psu=buildPart('psu'),chassis=buildPart('case');
 const checks=[],requiredMissing=buildSlotDefs.filter(s=>s.required&&!buildIds(s.id).length).map(s=>s.label);
 if(cpu&&board){const a=socketOf(cpu),b=socketOf(board);checks.push(!a||!b?checkResult('unknown','CPU socket','Socket data is incomplete.'):checkResult(a===b?'pass':'fail','CPU socket',a===b?`${a} processor and motherboard match.`:`${a} processor does not fit the ${b} motherboard.`))}else checks.push(checkResult('unknown','CPU socket','Select a processor and motherboard.'));
 if(board&&rams.length){const boardDDR=ddrOf(board),generations=rams.map(ddrOf),known=boardDDR&&generations.every(Boolean),matches=known&&generations.every(v=>v===boardDDR);checks.push(!known?checkResult('unknown','Memory generation','Memory-generation data is incomplete.'):checkResult(matches?'pass':'fail','Memory generation',matches?`All ${rams.length} memory kit${rams.length===1?'':'s'} use ${boardDDR}.`:`One or more kits do not match the motherboard's ${boardDDR} slots.`));const max=board.specs['Max RAM'],capacity=rams.reduce((sum,p)=>sum+(p.specs.Memory||0),0),capacityKnown=rams.every(p=>p.specs.Memory!==undefined);checks.push(max===undefined||!capacityKnown?checkResult('unknown','Memory capacity','Capacity limit is not fully indexed.'):checkResult(capacity<=max?'pass':'fail','Memory capacity',capacity<=max?`${capacity} GB is within the ${max} GB board limit.`:`${capacity} GB exceeds the ${max} GB board limit.`));const slots=board.specs['DIMM slots'],modules=rams.reduce((sum,p)=>sum+(p.specs.Modules||0),0),modulesKnown=rams.every(p=>p.specs.Modules!==undefined);checks.push(slots===undefined||!modulesKnown?checkResult('unknown','Memory slots','DIMM count is not fully indexed.'):checkResult(modules<=slots?'pass':'fail','Memory slots',modules<=slots?`${modules} modules use ${modules} of ${slots} DIMM slots.`:`${modules} modules require more than ${slots} DIMM slots.`))}else checks.push(checkResult('unknown','Memory support','Select a motherboard and at least one memory kit.'));
 const nvme=ssds.filter(p=>/NVMe/i.test(p.architecture)).length;if(nvme){const slots=board?.specs['M.2 slots'];checks.push(slots===undefined?checkResult('unknown','NVMe storage','Motherboard M.2 count is unavailable.'):checkResult(slots>=nvme?'pass':'fail','NVMe storage',slots>=nvme?`${nvme} drive${nvme===1?'':'s'} use ${nvme} of ${slots} M.2 slots.`:`${nvme} NVMe drives exceed ${slots} M.2 slots.`))}
 const legacy=[...ssds,...hdds].filter(p=>!/(NVMe|SATA)/i.test(p.architecture));if(board&&legacy.length)checks.push(checkResult('fail','Storage interface',`${legacy.map(p=>p.name).join(', ')} require a legacy controller not indexed on this motherboard.`));
 const sataDevices=[...ssds,...hdds].filter(p=>/SATA/i.test(p.architecture)).length;if(sataDevices){const ports=board?.specs['SATA ports'];checks.push(ports===undefined?checkResult('unknown','SATA storage','Motherboard SATA count is unavailable.'):checkResult(ports>=sataDevices?'pass':'fail','SATA storage',ports>=sataDevices?`${sataDevices} drives use ${sataDevices} of ${ports} SATA ports.`:`${sataDevices} drives exceed ${ports} SATA ports.`))}
 if(board&&chassis){const factor=formFactorOf(board);checks.push(!factor?checkResult('unknown','Case fit','Motherboard form factor is not indexed.'):checkResult(caseSupports(chassis,board)?'pass':'fail','Case fit',caseSupports(chassis,board)?`${factor} motherboard support is listed by the case family.`:`The ${factor} motherboard does not fit this case's indexed form-factor support.`))}else checks.push(checkResult('unknown','Case fit','Select a motherboard and PC case.'));
 const cpuPower=cpu?.specs['CPU TDP'],gpuPower=gpu?.specs['Board power'];const knownPower=cpuPower!==undefined&&(gpu?gpuPower!==undefined:true);const estimated=knownPower?cpuPower+(gpuPower||0)+100:null,recommended=estimated===null?null:Math.ceil(estimated*1.25/50)*50,rated=psu?.specs['Rated output'];checks.push(!psu?checkResult('unknown','Power supply','Select a power supply.'):recommended===null||rated===undefined?checkResult('unknown','Power headroom','Power specifications are incomplete.'):checkResult(rated>=recommended?'pass':'fail','Power headroom',rated>=recommended?`${rated} W exceeds the ${recommended} W planning target.`:`${rated} W is below the ${recommended} W planning target.`));
 const storage=[...ssds,...hdds].reduce((sum,p)=>sum+(p.specs['Storage capacity']||0),0),totalMemory=rams.reduce((sum,p)=>sum+(p.specs.Memory||0),0);const graphicsMetric=gpu?.specs['PassMark G3D']??gpu?.specs['Memory bandwidth'];const graphicsPool=catalog.filter(p=>p.cat==='gpu').map(p=>p.specs[gpu?.specs['PassMark G3D']!==undefined?'PassMark G3D':'Memory bandwidth']);const graphics=percentileScore(graphicsMetric,graphicsPool),cores=cpu?.specs['CPU cores'],boost=cpu?.specs['Boost clock'],processor=cores===undefined?null:Math.min(95,Math.round(32+cores*4+(boost||0)*5)),memory=rams.length&&rams.every(p=>p.specs.Memory!==undefined)?Math.min(95,Math.round(38+totalMemory*1.6)):null,storageScore=!ssds.length?35:ssds.some(p=>/NVMe/i.test(p.architecture))?Math.min(95,65+Math.max(...ssds.map(p=>p.specs['Sequential read']||0))/250):55;const knownScores=[graphics,processor,memory,storageScore].filter(Number.isFinite),profile=knownScores.length?Math.round((graphics??50)*.55+(processor??50)*.3+(memory??50)*.1+storageScore*.05):null;
 let target='Select a graphics card for a gaming outlook.';if(gpu){const bw=gpu.specs['Memory bandwidth']||0,vram=gpu.specs.Memory||0;target=bw>=700&&vram>=16?'4K-oriented graphics':bw>=450&&vram>=12?'High-refresh 1440p / tuned 4K':bw>=250&&vram>=8?'1080p high / 1440p gaming':'1080p and esports-oriented graphics'}
 const fail=checks.some(c=>c.state==='fail'),unknown=checks.some(c=>c.state==='unknown')||requiredMissing.length;return{checks,requiredMissing,verdict:fail?'fail':unknown?'unknown':'pass',estimated,recommended,rated,storage,totalMemory,graphics,processor,memory,storageScore,profile,target,parts:{cpu,board,gpu,rams,ssds,hdds,psu,case:chassis}};
}
function renderBuilder(){
 const wrap=$('#builder-parts');if(!wrap)return;const analysis=analyzeBuild();
 wrap.innerHTML=buildSlotDefs.map((slot,i)=>{const all=catalog.filter(p=>p.cat===slot.id),selectedParts=buildParts(slot.id),part=selectedParts[0],compatible=all.filter(p=>candidateCompatible(slot.id,p)),options=(compatibleOnly?all.filter(p=>candidateCompatible(slot.id,p)||selectedParts.some(s=>s.id===p.id)):all).filter(p=>!multiBuildSlots.has(slot.id)||!selectedParts.some(s=>s.id===p.id)).sort((a,b)=>(b.year||0)-(a.year||0)||a.name.localeCompare(b.name)),meta=slot.required?'Required':'Optional',state=selectedParts.length?`${selectedParts.length} SELECTED`:'EMPTY',control=multiBuildSlots.has(slot.id)?`<div class="builder-multi-control"><div class="builder-selected-items">${selectedParts.map(p=>`<span class="builder-selected-item"><span>${p.name}</span><button type="button" data-build-remove-slot="${slot.id}" data-build-remove-id="${p.id}" aria-label="Remove ${p.name}">×</button></span>`).join('')}</div><label class="sr-only" for="build-add-${slot.id}">Add another ${slot.label}</label><select id="build-add-${slot.id}" data-build-add="${slot.id}" aria-label="Add another ${slot.label}"><option value="">Add another ${slot.label.toLowerCase()}</option>${options.map(p=>`<option value="${p.id}">${p.name} · ${p.year||'year unlisted'}</option>`).join('')}</select></div>`:`<select data-build-slot="${slot.id}" aria-label="Choose ${slot.label}"><option value="">${slot.required?'Select a component':'None selected'}</option>${options.map(p=>`<option value="${p.id}" ${part?.id===p.id?'selected':''}>${p.name} · ${p.year||'year unlisted'}</option>`).join('')}</select>`;return `<div class="builder-row"><span class="builder-index">${String(i+1).padStart(2,'0')}</span><span class="builder-type">${icon(slot.id)}<span><strong>${slot.label}</strong><small>${meta} · ${compatibleOnly?compatible.length+' compatible':all.length+' records'}</small></span></span>${control}<span class="builder-row-state ${selectedParts.length?'chosen':''}">${state}</span></div>`}).join('');
 $('#enable-incompatible').checked=!compatibleOnly;$('#builder-filter-status').textContent=compatibleOnly?'Compatible catalog view':'All records · incompatibilities enabled';
 const verdictText=analysis.verdict==='pass'?'Compatible based on indexed specs':analysis.verdict==='fail'?'Compatibility issue found':'Build partially verified',verdictCopy=analysis.verdict==='pass'?'Every available automated check passes. Confirm physical clearances and connector details before buying.':analysis.verdict==='fail'?'Review the failed checks before choosing these parts.':analysis.requiredMissing.length?`Required next: ${analysis.requiredMissing.join(', ')}.`:'Some compatibility fields are unavailable in the catalog.';
 $('#build-verdict').innerHTML=`<span class="verdict-code">BUILD STATUS</span><div class="verdict-line"><span class="status-dot ${analysis.verdict}"></span><strong>${verdictText}</strong></div><p>${verdictCopy}</p>`;$('#build-checks').innerHTML=analysis.checks.map(c=>`<div class="check-row"><span class="check-mark ${c.state}">${c.state==='pass'?'✓':c.state==='fail'?'!':'?'}</span><span><strong>${c.label}</strong><small>${c.detail}</small></span></div>`).join('');
 const p=analysis.parts,specRows=[['Processor',p.cpu?`${number(p.cpu.specs['CPU cores'])} cores / ${number(p.cpu.specs['CPU threads'])} threads`:'—'],['Graphics',p.gpu?`${number(p.gpu.specs.Memory)} GB · ${number(p.gpu.specs['Memory bandwidth'])} GB/s`:'Integrated / not selected'],['System memory',p.rams.length?`${number(analysis.totalMemory)} GB across ${p.rams.length} kit${p.rams.length===1?'':'s'}`:'—'],['Solid-state drives',p.ssds.length?`${p.ssds.length} selected`:'—'],['Hard drives',p.hdds.length?`${p.hdds.length} selected`:'—'],['Total storage',analysis.storage?`${number(analysis.storage)} GB`:'—'],['Chassis',p.case?p.case.name:'—'],['Estimated load',analysis.estimated?`${number(analysis.estimated)} W`:'—'],['PSU headroom',analysis.rated&&analysis.estimated?`${number(analysis.rated-analysis.estimated)} W`:'—']];$('#build-specs').innerHTML=specRows.map(([k,v])=>`<div class="build-spec"><span>${k}</span><strong>${v}</strong></div>`).join('');
 const scores=[['Graphics',analysis.graphics],['Processor',analysis.processor],['Memory',analysis.memory],['Game storage',analysis.storageScore]];$('#build-performance').innerHTML=`<div class="performance-lead"><span>LIKELY TARGET</span><strong>${analysis.target}</strong></div>${analysis.profile!==null?`<div class="profile-score"><span>Catalog-relative profile</span><strong>${analysis.profile}<small>/100</small></strong></div>`:''}<div class="profile-bars">${scores.map(([label,score])=>`<div><span>${label}</span><span class="profile-track"><i style="width:${score||0}%"></i></span><strong>${score??'—'}</strong></div>`).join('')}</div><p class="performance-note">Derived from indexed GPU throughput or benchmark position, CPU core/clock specifications, combined memory capacity and the fastest selected storage interface. It predicts a hardware tier, not game FPS.</p>`;saveBuildURL();
}
function saveBuildURL(){const url=new URL(location.href),values=buildSlotDefs.map(s=>buildIds(s.id).join('~')).join(',');if(values.replaceAll(',',''))url.searchParams.set('build',values);else url.searchParams.delete('build');if(compatibleOnly)url.searchParams.delete('compat');else url.searchParams.set('compat','all');history.replaceState(null,'',url)}
